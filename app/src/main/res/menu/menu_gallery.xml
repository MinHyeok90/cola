<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_show_participants"
        android:title="@string/action_show_participants"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_edit_album_title"
        android:title="@string/action_edit_album"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_exit_album"
        android:title="@string/action_exit_album"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_invite"
        android:title="@string/action_invite"
        app:showAsAction="ifRoom"/>

</menu>